<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.howdoesthehumaneyeseethings">

    <!-- Permissions -->
    <!-- For Internet access (e.g., video streaming, online assets) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- For Camera use -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- For accessing media (images/videos) based on Android version -->
    <!-- For Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- For Android < 13 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--uses-feature
        android:name="android.hardware.camera.ar"
        android:required="true" /-->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HowDoesTheHumanEyeSeeThings"
        tools:targetApi="31">

        <meta-data android:name="com.google.ar.core" android:value="optional" />

        <!-- This is your launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.HowDoesTheHumanEyeSeeThings">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LearningActivity"
            android:exported="false" />
        <activity
            android:name=".LessonDetailActivity"
            android:exported="false" />
        <activity
            android:name=".DragAndDropTestActivity"
            android:exported="false" />
        <activity
            android:name=".QuizActivity"
            android:exported="false" />
        <activity
            android:name=".ARActivity"
            android:exported="false" />
    </application>

</manifest>